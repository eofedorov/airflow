Задача: {{ task }}

Вход (текст для извлечения):
{{ input }}

Извлеки сущности (entities): каждая с полями "type" и "value" (например component, version, steps, impact_type).
Краткое резюме (summary) — до 2000 символов.

Схема ответа (строго придерживайся):
{{ output_contract }}

Ответь только валидным JSON по этой схеме, без пояснений до или после.
